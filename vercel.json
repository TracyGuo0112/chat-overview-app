{
  "version": 2,
  "buildCommand": "npm install --prefix shadcn-admin-template && npm run build --prefix shadcn-admin-template",
  "outputDirectory": "shadcn-admin-template/dist",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
